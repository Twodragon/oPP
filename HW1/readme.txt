Для запуска всего запускаем скрипт up.sh (там мы спим 10 секунд между запуском postgre, rabbit и consumer, producer, потому что rabbit долго поднимается, и producer может попытаться послать сообщения на еще не существующую очередь).

input для producer-a берётся из соответствующего файла.

‘docker-compose exec db psql -U db -d db’ для просмотра созданной БД.



ДЗ Ефимочкина Сергея, студента 597 группы.
